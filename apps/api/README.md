# Renova API

API desenvolvida com Node.js e Express.

## Scripts

- `pnpm dev --filter @renova/api` - Inicia a API em modo desenvolvimento.
- `pnpm lint --filter @renova/api` - Executa o linting do projeto.
- `pnpm test --filter @renova/api` - Executa os testes automatizados.
